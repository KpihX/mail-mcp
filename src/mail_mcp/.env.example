# mail-mcp local overrides (optional — for dev/testing only)
# In production, secrets come from bw-env (GLOBAL_ENV_VARS in Bitwarden).
# Copy to .env and fill in to bypass the login-shell resolution tier.

# Polytechnique (X) account
X_LOGIN=prenom.nom              # e.g. ivann.kamdem-pouokam
X_PASS=your_ldap_password_here
